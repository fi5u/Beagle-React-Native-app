import { StyleSheet } from 'react-native'

const Styles = StyleSheet.create({
    base: {
        flex: 1,
    },
    header: {
        height: 50,
    },
    body: {
        flex: 1,
    },
})

export default Styles
